using UnityEngine;

namespace UserControllSystem.UI.Model
{
    [CreateAssetMenu(fileName = nameof(Vector3Value), menuName = "Strategy game/" + nameof(Vector3Value), order = 0)]
    public class Vector3Value : ObjectValueBase<Vector3> {}
}